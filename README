metrics

TODO:
1. register all meters (make reflections public static)
2. write aspectj aspects to count metrics
    counter
    gauge
    histogram
    summary
3. split prometheus and other implementations out into modules / plugins